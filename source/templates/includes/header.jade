header.header
  .logo
    a(href='/')= site.name

  .menu-toggle#menu
    .menu-icon

  .menu
    ul
      each page in header
        - var isCurrent = page.url == url ? 'current-page': '';
        li(class= isCurrent): a(href= page.url)= page.name
